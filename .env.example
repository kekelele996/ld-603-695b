# Compose 项目名（容器名统一此前缀，支持中文目录名启动）
COMPOSE_PROJECT_NAME=fire-inspect
# 前端宿主机访问端口（容器内固定 80）
FRONTEND_PORT=20103
# 后端宿主机访问端口（容器内固定 8000）
BACKEND_PORT=21103
# PostgreSQL 宿主机映射端口
DB_PORT=54320
# 数据库凭据
DB_NAME=app_db
DB_USER=app_user
DB_PASSWORD=app_password
